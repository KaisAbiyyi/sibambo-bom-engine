# Sibambo BOM Engine React UI Draft 04

This is UI Draft 04 for the Sibambo BOM Engine.

Concept: Top-Down Structural Inspection Board.

## Run

```bash
cd react-4
npm install
npm run dev
```

This draft:

- Does not modify `bom_engine_plugin`.
- Does not modify the Ruby exporter.
- Does not change JSON schema.
- Uses a copied local JSON file for UI draft display.
