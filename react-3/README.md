# Sibambo BOM Engine React UI Draft 03

This is UI Draft 03 for the Sibambo BOM Engine.

Concept: Spatial Digital Twin / Structural Assembly Lab.

## Run

```bash
cd react-3
npm install
npm run dev
```

This draft:

- Does not modify `bom_engine_plugin`.
- Does not modify the Ruby exporter.
- Does not change JSON schema.
- Uses a copied local JSON file for UI draft display.
