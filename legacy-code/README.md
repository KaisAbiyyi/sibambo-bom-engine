# Legacy Code

Restored legacy HTML files from Git history.

Source commit:

```text
a6bf3f638e387f52a6e32bf4aea7f635b9ad2225
Initial commit: BOM Engine SketchUp plugin + web viewers
```

## Files

| File | Original path | Purpose |
| --- | --- | --- |
| `model-viewer-old.html` | `house3d_viewer.html` | Original BOM Engine 3D model viewer HTML. |
| `model-editor-semi.html` | `house3d_viewer copy.html` | Legacy semi-editor / inspection variant with hierarchy, layers, materials, inspector, and section controls. |

## Complete HTML History

All `.html` files found in Git history are restored under:

```text
legacy-code/history/
```

The folder preserves original repository paths. See:

```text
legacy-code/history/MANIFEST.md
```

These files are kept as source snapshots only. Current production work should stay in `model-viewer/` and `model-eval/`.
