Optional default model loading:

Place `Model_SBMBOOST_bom_visual_nonPretty-print.json` or `Model_SBMBOOST_bom.json`
in this `public/` folder to let the app auto-load it during development.

Manual JSON upload and drag/drop are supported without copying files here.
